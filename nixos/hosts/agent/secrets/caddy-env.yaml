# STUB — needs `sops --encrypt --in-place` once age key is provisioned
# Required keys (consumed by Caddy via EnvironmentFile):
#   PORKBUN_API_KEY        — Porkbun API key for DNS-01 challenge
#   PORKBUN_SECRET_API_KEY — Porkbun secret API key for DNS-01 challenge
#
# Operator steps:
#   1. Provision /etc/sops/age/key.txt (see secrets.nix comment)
#   2. Fill in real values below
#   3. sops --encrypt --in-place --age "$(cat /etc/sops/age/key.txt | grep -v '#' | tr -d '\n')" nixos/hosts/agent/secrets/caddy-env.yaml
PORKBUN_API_KEY: "REPLACE-ME"
PORKBUN_SECRET_API_KEY: "REPLACE-ME"
