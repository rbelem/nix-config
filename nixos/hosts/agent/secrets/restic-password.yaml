# STUB — needs `sops --encrypt --in-place` once age key is provisioned
# Required key (consumed by restic via passwordFile):
#   RESTIC_PASSWORD — encryption password for the restic repository
#
# Operator steps:
#   1. Provision /etc/sops/age/key.txt (see secrets.nix comment)
#   2. Fill in real value below
#   3. sops --encrypt --in-place --age "$(cat /etc/sops/age/key.txt | grep -v '#' | tr -d '\n')" nixos/hosts/agent/secrets/restic-password.yaml
RESTIC_PASSWORD: "REPLACE-ME"
