# STUB — needs `sops --encrypt --in-place` once age key is provisioned
# Required keys (consumed by restic via environmentFile):
#   AWS_ACCESS_KEY_ID     — Hetzner Object Storage access key
#   AWS_SECRET_ACCESS_KEY — Hetzner Object Storage secret key
#
# Operator steps:
#   1. Provision /etc/sops/age/key.txt (see secrets.nix comment)
#   2. Fill in real values below
#   3. sops --encrypt --in-place --age "$(cat /etc/sops/age/key.txt | grep -v '#' | tr -d '\n')" nixos/hosts/agent/secrets/restic-env.yaml
AWS_ACCESS_KEY_ID: "REPLACE-ME"
AWS_SECRET_ACCESS_KEY: "REPLACE-ME"
